# AIGC 知识付费站 · 网站规划

更新：2026-09-06
路径：`/Users/himma/01/01Agent学习/aigc知识付费`

## 一句话

一个**自己上传文档和案例、按篇定价、按分组给权益**的内容站。
不是 LMS：没有课程进度、作业、评分、直播。内容源就是手上已有的 Markdown。

## 要解决的三件事

1. **上传**：把 Markdown / 图片 / PDF 放上去，分类、打标签、发布。
2. **定价**：每篇可以「免费 / 收费（单价）/ 仅专属组可见」，收费文章前半段可预览。
3. **分组**：建一个组（某班、某公司、VIP、老学员），拉人进来，这个组对全站或某些文章「免费 / 打折 / 专属可见」。

## 角色

- 管理员（我）：上传、定价、建组、发码、看订单。
- 访客：看免费文章，看收费文章的预览。
- 注册用户：买单篇、输兑换码、点邀请链接进组。
- 分组成员：按组策略读文章；一个人可以在多个组，取最优惠的那条。

## 内容模型

- 两种内容：**文档**（教程、指南、读本）和**案例**（背景 / 工具 / 提示词 / 产出 / 复盘，用固定模板）。
- 正文 Markdown；图片粘贴即上传；附件（PDF、源文件）挂在文章下。
- 预览分割线 `<!-- more -->`：以上部分对所有人可见，以下部分只给有权限的人。没写分割线默认取前 30%。
- 每篇：分类（一级 / 二级）、标签、免费 / 收费 / 专属、单价、草稿 / 已发布 / 下架。
- 支持 front-matter 批量导入，先把 `01Agent学习/*.md`、`04知识付费/lessons/*.md` 挑一批灌进去：

```yaml
---
title: Claude Code 从零到精通
category: Agent工程
tags: [Claude Code, CLI]
kind: doc          # doc | case
access: paid       # free | paid | exclusive
price: 29          # 元；free 时忽略
groups: [vip]      # exclusive 时哪些组可见
---
```

## 分组模型

一个组 = 名字 + 默认策略 + 有效期 + 邀请码（可限名额）+ 成员（每人可单独设到期日）+ 特例规则。

- **默认策略**：无 / 全站免费 / 全站折扣 x%。
- **特例规则**（针对某分类或某篇）：免费 / 折扣 x% / 专属可见。
- **优先级**：针对文章的规则 > 针对分类的规则 > 组默认。
- 常见用法：
  - 某班：辅导员买了入门轨 → 组规则「入门 分类免费」，其他打 5 折。
  - 某公司内训：全站免费，有效期到合同结束。
  - VIP 陪跑：专属案例只有这组能看到 + 全站免费。
  - 老学员：全站 7 折，不过期。
- 入组方式：邀请链接 `/join/<码>`、注册时填邀请码、后台手动加 / CSV 导入。

## 权限规则（唯一真相，前后端都从这一处算）

```text
可见(用户, 文章):
  文章不是专属           → 可见
  文章专属               → 用户在任一「专属可见」组且未过期 → 可见；否则 404，列表里也不出现

能读全文(用户, 文章):
  文章免费               → 是
  未登录                 → 否（只给预览）
  管理员                 → 是
  已购买 / 已兑换 / 后台手动开通，且未过期 → 是
  用户任一有效组命中「免费」（按 文章 > 分类 > 组默认）→ 是
  否则                   → 否

价格(用户, 文章):
  基价 = 文章单价
  遍历用户有效组命中的「折扣 x%」，取最低价
  已能读全文时不显示价格
```

组过期或成员到期，权益立刻消失；已购买的单篇永久有效。

## 功能清单

### 前台

| 页面 | 内容 |
|------|------|
| `/` | 精选、最新、分类入口 |
| `/c/<分类>` | 列表；卡片标「免费 / ¥xx / 组内免费 / 组内 ¥xx」 |
| `/a/<slug>` | 免费全文；收费显示预览 + 付费墙卡片（原价、我的折后价、去购买 / 输兑换码）；有权限直接全文 |
| `/search` | 标题 / 摘要 / 标签 |
| `/login` `/register` | 邮箱或手机 + 密码；注册时可填邀请码直接进组 |
| `/join/<邀请码>` | 一键进组，未登录先登录 |
| `/me` | 我买过的、我所在的组（到期日）、输兑换码、改密码 |
| `/buy/<id>` | 下单 → 支付。第 1 版：收款码 + 订单号备注，后台点「已到账」；第 3 版换在线支付 |

### 后台 `/admin`

- **内容**：新建 / 编辑（Markdown 编辑器 + 实时预览、图片粘贴上传、附件）、分类标签、预览分割线、免费 / 收费 / 专属 + 单价、发布 / 下架、批量导入本地 `.md` 目录。
- **分组**：新建、默认策略、有效期、邀请码 + 名额、成员增删 / 设到期日 / CSV 导入、特例规则（分类或文章 × 免费 / 折扣 / 专属）。
- **用户**：列表搜索、设管理员、手动开通某篇、手动入组、停用。
- **订单**：列表、手动确认到账、标记退款。
- **兑换码**：按文章 / 按组 / 全站生成批次，次数、有效期、导出。
- **数据**：阅读量、预览 → 购买转化、收入、各组活跃。

## 数据表（够第 0–2 步用）

```text
users           id, email/phone, password_hash, nickname, role(admin|member), status, created_at
categories      id, name, slug, parent_id, sort
articles        id, title, slug, summary, cover, body_md, kind(doc|case), category_id,
                access(free|paid|exclusive), price_cents, status(draft|published|off), published_at, view_count
tags / article_tags
attachments     id, article_id, filename, path, size, mime
groups          id, name, code, default_mode(none|all_free|discount), discount_percent,
                valid_from, valid_to, invite_code, seat_limit
group_members   group_id, user_id, joined_at, expires_at
group_rules     group_id, target_type(article|category), target_id, mode(free|discount|exclusive), discount_percent
entitlements    id, user_id, article_id, source(purchase|redeem|admin), order_id, expires_at
orders          id, user_id, article_id, list_cents, pay_cents, discount_source, status(pending|paid|refunded),
                channel, out_trade_no, paid_at
redeem_codes    code, kind(article|group|all), target_id, uses_left, expires_at, created_by
audit_logs      actor_id, action, target, at
```

## 技术选型

**推荐**：Next.js（App Router, TypeScript）+ PostgreSQL + Drizzle + Tailwind；Markdown 用 remark + shiki；文件先存本机 `data/uploads`，后期换 OSS / MinIO；登录用 cookie session。

理由：

- 免费文章要被搜到（引流），SSR 直接出 HTML。
- 付费正文在**服务端**裁掉再返回，浏览器拿不到没权限的部分。
- 前台、后台、接口一套代码一个进程，一个人维护得动。

**备选**：FastAPI + React（同 `vibe-coding-platform`）。手感熟、起得快，但 SPA 对 SEO 弱，正文裁切要单独做接口。

**不自建的选项**（诚实对比）：

- Ghost：原生会员 + 付费层 + 按层可见，但走 Stripe、没有单篇购买、没有按组折扣。
- 小报童 / 知识星球：今天就能开卖，但没有分组折扣，内容在别人平台。

## 支付

- **第 1 版不接支付 SDK**：兑换码 + 收款码（订单号备注 → 后台确认）。分组本来就是「一个人（辅导员 / 公司）付一次，一群人用」，管理员建组发邀请码即可覆盖大部分成交。
- **第 3 版**：微信支付 / 支付宝官方商户需要主体资质（个体户 / 公司）+ 备案域名（微信 H5 支付要求）；个人走聚合支付有费率和合规风险，自己评估；海外用户用 Stripe。

## 防盗（够用即可）

- 正文服务端裁切，未授权不下发。
- 付费页正文底纹水印：昵称 / 手机尾号。
- 附件走一次性签名链接；PDF 加水印（后期）。
- 同账号并发登录数限制（后期）。
- 不做 DRM、不做禁复制：无效且伤体验。

## 部署

- 一台独立 VPS + Docker Compose：`web`（Next）+ `pg` + `caddy`（自动 HTTPS）。
- **不要放到 87 的 Caddy 上**（那台 80/443 是中转站的）。
- 国内学生为主：国内云 + 备案体验最好，且微信支付要备案域名；备案 2–4 周，先用境外 VPS 起来，备案并行做。
- 备份：每日 `pg_dump` + `uploads/` 同步到另一处。
- 密钥只在 `.env`，不进仓库。

## 分步

| 步 | 目标 | 交付 |
|----|------|------|
| 0 | 能看 | 骨架 + 数据表 + 导入本地 `.md` + 前台阅读（全免费）+ 后台登录 |
| 1 | 能收 | 注册登录 + 免费 / 收费 + 预览裁切 + 兑换码 + 人工确认订单 + 我的购买 |
| 2 | 能分组 | 建组 / 邀请码入组 / 全免 / 折扣 / 专属 / 特例规则 + 成员管理 |
| 3 | 能上线 | 域名 HTTPS + Compose + 备份 + sitemap + 水印 + 数据看板 |
| 4 | 要了再做 | 在线支付、会员（月 / 年）、评论 / 提问、案例投稿、视频 |

每步 2–3 天，第 0–2 步做完就能私下开卖。

## 第 2 步结束时要过的场景

1. 导入 `01Agent学习` 15 篇，标 5 篇免费 10 篇收费。
2. 访客打开收费文章只看到预览 + 价格。
3. 学生 A 注册、输兑换码，读到全文。
4. 建「某班」组，全站 5 折，发邀请链接；学生 B 进组后收费文章显示折后价。
5. 建「某公司」组，规则「案例分类免费」；成员 C 打开案例是全文，打开其他收费文章仍要付费。
6. 建「VIP」组，专属文章 D 只有 VIP 可见；非成员访问 404，列表里不出现。
7. 把「某班」组设为已过期，B 的折扣立刻消失，B 之前买过的单篇仍能读。

## 不做 / 不要

- 课程进度、作业、评分、直播（LMS）。
- 自建生成 / 练习场：用 nornless + 无限画布。
- 第 1 版接支付 SDK。
- 上 87 的 Caddy。
- DRM / 禁复制。
- 讲义红线沿用 `04知识付费`：机场、灰 Key、网关站名不进公开文章。

## 开发方式：DeepSeek Harness 的两种用法

- **A. 当施工队**：DeepSeek Harness（`dsh`，2026-08-13 开源，MIT）是编码代理运行时——Web UI + 文件 / 命令工具 + 计划模式 + 子代理 + 审批，相当于开源的 Claude Code / Codex。它读本目录的 `AGENTS.md` 和两份规划，按步骤生成 `web/`（Next.js）。站和 dsh 无关。
- **B. 当脚手架**：整站就是一棵 dsh 插件树——内容、权限、付费墙、分组、后台、AI 助教、学员侧技能各是一个 Cordis 插件，装进自定义 profile；公共站的 HTML 由 dsh 的 HTTP 载体直接出，学员本机的 dsh 装上我们的 bundle 就能把已解锁内容当技能用。方案、插件清单、配置样例、分步见 **`02-DeepSeek-Harness集成.md`**。

选哪条看产品：只卖文章 + 付费墙，A 更省；要「内容进代理 + 助教」，B 净赚。两条路的功能、数据、权限规则（本文前半）和视觉（`01`）完全相同，只有代码落点不同。

### A 路怎么用 dsh

本机目前没装 dsh（`~/.dsh`、`dsh` 都不在）。

1. 装：`git clone https://github.com/deepseek-ai/deepseek-harness && cd deepseek-harness && pnpm install && pnpm run build`，然后 `pnpm dsh web` → `http://127.0.0.1:3080`。
2. 模型：Settings → Models。自定义 OpenAI 兼容提供方指向 `https://api.nornless.com/v1`，配置样例见知识库 [[中转站]]「DeepSeek Harness 接 Grok」；写代码用 `grok-build-0.1`，对话用 `grok-4.6`。也可以直接填 DeepSeek 官方 Key。Key 只填在 Web UI，不进文件。
3. 工作区：Choose workspace → 选本目录 `aigc知识付费/`。它会自动读到 `AGENTS.md`。
4. 第一轮用 Plan 模式：「读 00 和 01，按第 0 步给实施计划，列出会建哪些文件、跑哪些命令，先不动手」。看完计划再切 Standard 模式放行。
5. 每步一个 session，一步验收通过再开下一步；审批保持默认（写文件 / 跑命令要确认），不要一上来全放行。
6. dsh 是 developer preview，接口会变；卡住就换 Cursor / Claude Code / Codex 继续——两份规划和 `AGENTS.md` 对它们同样有效。

## 目录约定

```text
aigc知识付费/
  00-网站规划.md      本文：功能 / 数据 / 权限 / 分步
  01-页面设计.md      风格变种 / 令牌 / 版式 / 每页线框
  02-DeepSeek-Harness集成.md   B 路：dsh 为脚手架的插件清单 / 配置 / 分步
  AGENTS.md           给编码代理的规矩（dsh 自动读取）
  wireframes/         静态线框：nornless.css 令牌真源 + 6 页 HTML，index.html 是索引
  content/            待导入的 Markdown 源（从 01Agent学习、04知识付费 挑）
  web/                A 路网站代码（第 0 步建）
  harness/            B 路 dsh 插件 workspace（H0 建）；skills/ 放每篇的 SKILL.md
```

## 待定（开工前拍板）

0. 架构：A 施工队（Next.js 站）还是 B 脚手架（dsh 插件树，`02`）。
1. 栈（A 路才需要）：Next.js 还是 FastAPI + React。
2. 账号标识：邮箱还是手机号（手机号要接短信，先邮箱最省事）。
3. 收款主体：有没有个体户 / 公司；没有就先兑换码 + 收款码。
4. 域名与服务器：国内备案还是先境外。
