# AIGC 知识付费站 · 页面设计

更新：2026-09-06
配套：`00-网站规划.md`（功能与数据）、`wireframes/`（可点开的静态线框，含浅色 / 深色切换）

## 风格：nornless「书页」变种

沿用 `api.nornless.com` 的视觉系统（令牌源 `sub2api/frontend/src/theme/nornless.ts`），做一个**给长文阅读**用的变种，代号「书页」。

不变的：

- 浅色 = 未写过的羊皮纸 `#f6f1e6` + 金线；深色 = 命运之井 `#0b0a14` + 同一根金线，辅色靛蓝极光 `#58557f`。
- 三团柔光底纹（金 / 极光 / 线），卡片圆角 16、1px 描边、重点卡顶部 3px 金线。
- 小号大写字距标签（kicker），金渐变主按钮，遮罩弹窗做写操作。
- `html.dark` 切换，`localStorage.theme`，顶栏可切。
- 退役青绿 `#14b8a6` 不出现。

为阅读改的：

- 前台**没有侧栏**：顶栏 + 内容列；后台保留 256px 侧栏（同 vibe-coding-platform）。
- 标题用宋体系（`Noto Serif SC` / `Songti SC`），正文和界面仍是系统黑体；后台全部黑体。
- 正文列 720px，17px / 1.85 行高；右侧 280px 栏放目录和购买卡。
- **金线 = 内容的线**：文章页顶部 2px 金线是阅读进度；付费墙处金线断开、末端一个亮点（logo 里那根未闭合的线），文案「线到此处」。
- 三种语义色：**金 = 付费 / 价值**，**靛蓝极光 = 分组 / 归属**，**中性 = 免费**。

## 令牌

真源文件：`wireframes/nornless.css`（开工后原样搬到 `web/app/globals.css`）。

| 变量 | 浅色 | 深色 | 用途 |
|------|------|------|------|
| `--paper` | `#f6f1e6` | `#0b0a14` | 页面底 |
| `--paper-soft` | `#faf6ee` | `#161528` | 次级底、空态 |
| `--card` | `#ffffff` | `#161528` | 卡片 |
| `--ink` | `#1c1724` | `#ece7d8` | 正文 |
| `--muted` | `#5c5648` | `#9e9bbd` | 次要文字 |
| `--line` | `#e6ddd0` | `#343250` | 描边 |
| `--hairline` | `#f3f4f6` | `#232238` | 更淡的分隔 |
| `--primary` | `#8b6914` | `#c49632` | 金线正文色、链接 |
| `--primary-mid` | `#a67c1a` | `#d4b15c` | 焦点环、装饰 |
| `--primary-bright` | `#c49632` | `#e6cd8f` | 卡顶金线、进度线 |
| `--primary-deep` | `#6e5210` | `#a67c1a` | 按钮渐变深端 |
| `--primary-soft` | `#fbf6ea` | `rgba(166,124,26,.22)` | 金色底 chip |
| `--primary-100` | `#f3e6c4` | `rgba(212,177,92,.28)` | 悬停描边 |
| `--accent` | `#58557f` | `#c7c5db` | 分组语义文字 |
| `--accent-soft` | `#e4e3ef` | `rgba(88,85,127,.35)` | 分组语义底 |
| `--well` | `#0b0a14` | `#0b0a14` | 代码块、专属 chip 底 |
| `--ok` / `--warn` / `--danger` | `#059669` / `#d97706` / `#dc2626` | 同 | 状态 |
| `--topbar` | `rgba(255,255,255,.72)` | `rgba(22,21,40,.78)` | 顶栏毛玻璃 |

对比度（已算）：浅色金字 `#8b6914` 在羊皮纸上 4.5:1、在白卡上 5.1:1，**正文级金字只用 600 档**；`#a67c1a` 3.4:1 只做线和大字。深色 `#c49632` 在井底 7.3:1、在卡上 6.6:1，随便用。

## 字体与字号

- 界面：`system-ui, -apple-system, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif`
- 标题（仅前台 h1 / h2 / 品牌字）：`"Noto Serif SC", "Source Han Serif SC", "Songti SC", serif`，字重 600
- 代码：`ui-monospace, Menlo, monospace`
- 阶梯：kicker 12/0.14em 大写 · 小字 13 · 界面 14 · 正文 17（移动 16） · h3 18 · h2 24 · h1 32（首页 40）
- 正文行高 1.85，段距 1em，不两端对齐。

## 版式

- 前台顶栏 64px：左 品牌（金线小方 28px + 宋体站名）· 中 首页 / 文档 / 案例 · 右 搜索 / 主题 / 登录或头像。底部 1px `--hairline`。文章页下方再加 2px 金色进度线。
- 列表容器 1120px；文章页 `720 + 48 + 280`，右栏 `position: sticky; top: 88px`。
- 后台 `grid-template-columns: 256px 1fr`；顶条 64px 显示当前模块和管理员。
- 断点：1100 四列变两列；900 前台右栏落到正文下、后台侧栏折成顶部横排；600 单列，文章页出**底部吸底购买条**（微信内浏览器主要靠这条）。

## 组件

- **访问 chip**：`免费`（透明底 + 描边 + 灰字）· `¥29`（金底金字）· `组内免费` / `组内 ¥14.5`（极光底靛蓝字）· `专属`（井底金字）· `已解锁`（绿底绿字）。列表卡右上角一枚，文章标题下一枚。
- **按钮**：主 = 金渐变白字；次 = 卡底描边；幽灵 = 透明。`small` 用在表格内。
- **卡片**：默认 1px 描边；`featured` 顶部 3px `--primary-bright`；悬停描边变 `--primary-100` 并抬阴影。
- **金线断点** `.thread-break`：一根从左到 62% 的金渐变线，末端一个 8px 亮点；只在付费墙用。
- **付费墙卡**：预览最后三行渐隐 → 金线断点 → 卡：「线到此处」kicker、原价（有折扣时划掉）、你的价格 + 折扣来源 chip、主按钮「解锁全文 ¥xx」、次按钮「输入兑换码」、小字「已购永久可读 · 组权益随组有效期」。未登录时主按钮换成「登录后看你的价格」。
- **已解锁条**：文章标题下方一行，`已解锁 · 来自「某班」 · 到期 2026-12-31`，或 `已购买 · 永久`。
- **兑换码框**：单行输入 + 按钮，成功后原地变成绿色已解锁条，不跳页。
- **表单**：12px 圆角输入，焦点金环 `0 0 0 3px rgba(166,124,26,.18)`。
- **表格**：表头 12px 大写灰字，行 hover 金色浅底；可点行整行可点。
- **空态**：虚线框 + 一句话 + 一个动作按钮。
- **弹窗**：遮罩 `rgba(11,10,20,.52)`，卡顶 3px 金线；写操作都走弹窗。
- **主题切换**：顶栏一枚，图标 + 「浅 / 深」，写 `localStorage.theme`。

## 前台页面

### `/` 首页

```
┌ 顶栏 ───────────────────────────────────────────────┐
│ ◆ AIGC 知识库      首页  文档  案例        🔍 ☾ 登录 │
├─────────────────────────────────────────────────────┤
│ kicker: 文档 · 案例 · 分组                            │
│ H1 宋体：把 AI 用在真活上                             │
│ 一句话 + [开始阅读] [输入兑换码 / 邀请码]              │
├─ 精选（3 张 featured 卡，顶部金线）───────────────────┤
│ [封面] 标题 · 摘要 · 分类 · chip                      │
├─ 分类入口（4 格：校园使用 / 认知原理 / Agent 工程 / AIGC 生产）│
├─ 最新（列表卡，两列）─────────────────────────────────┤
└─ 页脚：站名 · 关于 · 联系 · 备案号（有则显示） ────────┘
```

未登录看到的就是这个。登录后 hero 换成「继续阅读」最近一篇 + 我的分组 chip。

### `/c/<分类>` 分类列表

左 2/3 列表卡（标题、摘要、标签、chip、更新时间），右 1/3 侧栏：子分类、标签云、「这一类对你的价格」说明（有组折扣时显示「某班 · 全站 5 折」）。顶部筛选条：全部 / 免费 / 已解锁 / 文档 / 案例。

### `/a/<slug>` 文章

三种态，一套版式：

```
┌ 顶栏 + 2px 金色进度线 ─────────────────────────────┐
│ 面包屑  首页 / Agent 工程                            │
│ H1 宋体标题                                          │
│ chip ¥29 · 分类 · 更新 2026-09-04 · 约 18 分钟        │
│ [已解锁条：来自「某班」 · 到期 2026-12-31]（有则显示）  │
├──────────── 720 ─────────────┬──── 280 sticky ─────┤
│ 正文 prose                    │ 目录 TOC              │
│ ...                           │ ─────────             │
│ （付费墙态）最后三行渐隐        │ 购买卡：原价 / 你的价  │
│ ── 金线断点 ●                  │ [解锁全文] [兑换码]    │
│ ┌ 付费墙卡 ─────────────────┐ │ 附件（锁 / 可下载）    │
│ │ 线到此处                   │ │ 分享                  │
│ │ ¥29 → ¥14.5 「某班 · 5 折」│ │                       │
│ │ [解锁全文 ¥14.5] [兑换码]  │ │                       │
│ └───────────────────────────┘ │                       │
├─ 相关文章 3 张 ──────────────────────────────────────┤
└──────────────────────────────────────────────────────┘
移动端：右栏消失，底部吸底条「¥14.5 · 某班 5 折  [解锁全文]」
```

- 免费态：无购买卡，右栏只有目录 + 附件。
- 已解锁态：标题下已解锁条，右栏购买卡换成「已解锁」+ 附件下载。
- 专属且无权：直接 404 页（「这一页不存在」），列表也不出现。

### `/search`

顶部大输入框，下方结果列表卡；结果卡同分类列表卡。无结果空态给「按分类浏览」。

### `/login` `/register`

居中 440px 卡，顶 3px 金线（同 vibe 登录页）：品牌小方 + kicker + 标题。注册多一格「邀请码（选填）」，填了注册后直接进组并提示。底部主题切换。

### `/join/<邀请码>`

居中卡：组名、组说明、这组能得到什么（全站免费 / 5 折 / 专属案例）、有效期、剩余名额；按钮「加入」；未登录先跳登录再回来。已在组内显示「你已在这个组」。

### `/me`

左侧小导航（我的购买 / 我的分组 / 兑换码 / 账号），右侧内容：

- 我的购买：列表（标题、购买时间、金额、去阅读）。
- 我的分组：卡片（组名、策略摘要、到期日、过期灰显）。
- 兑换码：一个输入框。
- 账号：昵称、邮箱、改密码、登出。

### `/buy/<id>`

第 1 版：居中卡：文章标题、价格明细（原价、折扣、应付）、订单号、收款码图、「我已付款」按钮 → 状态「等待确认」；后台确认后自动变「已解锁，去阅读」。第 3 版换成拉起微信 / 支付宝。

## 后台页面 `/admin`

侧栏分组：

- 内容 → 文章 · 分类标签 · 导入
- 用户与分组 → 分组 · 用户
- 交易 → 订单 · 兑换码
- 数据 → 总览

### 总览

四格统计（今日阅读 / 本月收入 / 待确认订单 / 活跃组），下方两卡：预览 → 购买转化曲线、待办（待确认订单列表）。

### 文章列表

筛选条（搜索 / 分类 / 访问 / 状态）+ 表格（标题、分类、访问 chip、价格、状态、更新、操作）。右上「新建」「导入目录」。

### 文章编辑

```
┌ 顶条：← 返回 · 标题输入 · [保存草稿] [预览] [发布] ────┐
├───────── 左：Markdown 编辑 ─────────┬── 右：实时预览 ──┤
│ 工具条：图片 · 附件 · 插入 <!-- more --> │                  │
│ textarea / CodeMirror               │ prose 渲染        │
├─ 底部折叠面板「发布设置」───────────────────────────────┤
│ 分类 ▾  标签 [ ]  类型 (文档|案例)  slug  摘要           │
│ 访问 (免费|收费|专属)  单价 ¥[ ]  专属可见组 [多选]       │
│ 封面 [上传]  附件列表（文件名 · 大小 · 删除）             │
└──────────────────────────────────────────────────────┘
```

图片粘贴即上传并插入 Markdown 链接。案例类型时编辑区预填模板：背景 / 工具 / 提示词 / 产出 / 复盘。

### 分组列表

表格：组名、编码、默认策略、成员数、有效期、状态 chip、操作。右上「新建分组」弹窗。

### 分组详情

```
┌ 组名  编码 chip  状态 chip（有效 / 已过期）  有效期  [编辑] ┐
├ Tab：策略 · 成员 · 特例规则 · 邀请码 ──────────────────────┤
│ 策略：分段选择 无 / 全站免费 / 全站折扣 [  ]%                │
│ 成员：表格（昵称 · 邮箱 · 加入 · 到期 · 移除）[手动添加][CSV] │
│ 规则：表格（目标类型 · 目标 · 模式 · 折扣 · 删除）[新增规则]  │
│ 邀请码：码 · 链接复制 · 已用 / 名额 · 有效期 · [重置]         │
└─────────────────────────────────────────────────────────────┘
```

### 用户

表格（昵称、邮箱、角色、所在组、注册时间、状态、操作）。行操作：手动开通某篇、加入某组、停用。

### 订单

筛选（状态 / 时间）+ 表格（订单号、用户、文章、原价、实付、来源、状态、时间、操作「确认到账」「退款」）。待确认默认置顶。

### 兑换码

「生成批次」弹窗：类型（文章 / 分组 / 全站）、目标、数量、每码次数、有效期。列表按批次折叠，可导出 CSV。

## 状态与文案

- 加载：卡片骨架，不转圈。
- 空态：一句话 + 动作。「还没有文章 → 新建 / 导入」「这个组还没人 → 复制邀请链接」。
- 组过期：文章页 chip 变灰「某班 · 已过期」，价格回原价，不弹窗。
- 兑换成功：原地变绿条，正文立即展开。
- 错误：卡内红字，不用 alert。

## 不要

- 前台加侧栏、加仪表盘感。
- 金字用 500 档 `#a67c1a` 排正文。
- 专属文章给非成员看到「无权限」页；应是 404。
- 付费墙用模糊遮罩糊住正文；预览就是真的前几段，其余服务端不下发。
- 用禁止复制、禁右键。

## 待定

- 站名（线框里暂用「AIGC 知识库」）。
- 首页是否要放价格总表；建议不放，价格只在文章页出现。
